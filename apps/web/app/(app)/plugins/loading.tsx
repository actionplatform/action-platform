import { PageSkeleton } from "@/components/ui/skeleton";

export default function Loading() {
  return <PageSkeleton title="Plugins" description="Deploy targets, overlays, tools and release strategies." variant="cards" label="Loading plugins" />;
}
