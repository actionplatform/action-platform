import { PageSkeleton } from "@/components/ui/skeleton";

export default function Loading() {
  return <PageSkeleton title="New app" description="Pick a template, configure the project and create it." variant="form" />;
}
