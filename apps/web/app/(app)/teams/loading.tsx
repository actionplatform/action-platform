import { PageSkeleton } from "@/components/ui/skeleton";

export default function Loading() {
  return <PageSkeleton title="Teams" description="Groups of members and the projects they look after." variant="cards" label="Loading teams" />;
}
